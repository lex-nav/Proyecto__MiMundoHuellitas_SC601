@model MiMundoHuellitas.Models.Mascota
@{
    Layout = "~/Views/Shared/_LayoutInterno.cshtml";
}

<table class="table table-hover">
    <thead>
        <tr>
            <th>#</th>
            <th>Nombre</th>
            <th>Tipo</th>
            <th>Raza</th>
            <th>Edad</th>
            <th>Estado</th>

        </tr>
    </thead>

    <tbody>
        @*@foreach (var item in Model)
            {
                <tr>
                    <td>@item.ConsecutivoMascota</td>
                    <td>@item.Nombre</td>
                    <td>@item.Tipo</td>
                    <td>@item.Raza</td>
                    <td>@item.Edad</td>
                    <td>@item.Estado</td>
                </tr>
            }*@
    </tbody>
</table>