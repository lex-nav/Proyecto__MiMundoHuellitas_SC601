@model MiMundoHuellitas.Models.Mascota
@{
    Layout = "~/Views/Shared/_LayoutInterno.cshtml";
}

<table class="table table-hover">
    <thead>
        <tr>
            <th>#</th>
            <th>Nombre</th>
            <th>Tipo</th>
            <th>Raza</th>
            <th>Edad</th>
            <th>Estado</th>
            <th style="width:160px;">Acciones</th>
        </tr>
    </thead>
    <tbody>
        @*@foreach (var m in Model)
            {
                <tr>
                    <td>@m.Id</td>
                    <td>@m.Nombre</td>
                    <td>@m.Tipo</td>
                    <td>@m.Raza</td>
                    <td>@m.Edad</td>
                    <td>@m.Estado</td>
                    <td>
                        <a href="@Url.Action("Edit", "Mascotas", new { id = m.Id })"
                           class="btn btn-sm btn-warning">Editar</a>

                        <button class="btn btn-sm btn-danger"
                                data-bs-toggle="modal"
                                data-bs-target="#modalEliminar"
                                data-id="@m.Id">
                            Eliminar
                        </button>
                    </td>
                </tr>
            }*@
    </tbody>
</table>

<!-- AQUI va el modal -->
@* Modal de confirmación *@
<div class="modal fade" id="modalEliminar" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Eliminar Mascota</h5>
            </div>
            <div class="modal-body">
                ¿Seguro que deseas eliminar esta mascota?
            </div>
            <div class="modal-footer">
                <form method="post" action="/Mascota/DeleteConfirmed">
                    <input type="hidden" id="idMascota" name="id" />
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                    <button type="submit" class="btn btn-danger">Eliminar</button>
                </form>
            </div>
        </div>
    </div>
</div>