<<<<<<< HEAD
﻿@using System.Web.Security

@{
    bool isAuth = (Request != null && Request.IsAuthenticated);

    string rol = "";

    if (User.Identity.IsAuthenticated)
    {
        var identity = (FormsIdentity)User.Identity;
        var ticket = identity.Ticket;
        rol = ticket.UserData; // 👈 AQUÍ viene "Admin", "Empleado", "Cliente"
    }

    bool esAdmin = rol == "Admin";
    bool esEmpleado = rol == "Empleado";
    bool esCliente = rol == "Cliente";
=======
﻿@{
    bool isAuth = User.Identity.IsAuthenticated;

    int? idTipoUsuario = null;

    if (Session["IdTipoUsuario"] != null)
    {
        idTipoUsuario = Convert.ToInt32(Session["IdTipoUsuario"]);
    }

    bool esCliente = idTipoUsuario == 1;
    bool esAdmin = idTipoUsuario == 2;
    bool esEmpleado = idTipoUsuario == 3;
>>>>>>> Sebas
}

@*bool isAuth = (Request != null && Request.IsAuthenticated);


        bool esAdmin = (User != null && User.Identity != null && User.Identity.IsAuthenticated &&
                        (User.IsInRole("Admin") || User.IsInRole("Administrador")));


        bool esEmpleado = (User != null && User.Identity != null && User.Identity.IsAuthenticated &&
                           User.IsInRole("Empleado"));
    }*@

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - MiMundoHuellitas</title>

    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")

    <link href="~/StaticFiles/css/style.css" rel="stylesheet" />
    <link href="~/StaticFiles/css/responsive.css" rel="stylesheet" />
    <link href="~/StaticFiles/CSS/mimundo.css" rel="stylesheet" />
    <link rel="stylesheet" href="~/StaticFiles/CSS/animaciones.css" />
    @RenderSection("Styles", required: false)

    <link rel="stylesheet"
          href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
          crossorigin="anonymous" referrerpolicy="no-referrer" />

    <style>
        html, body {
            height: 100%;
        }

        body {
            display: flex;
            flex-direction: column;
        }

        main.container-fluid.body-content {
            flex: 1;
        }

        .top-contact-bar {
            background: linear-gradient(90deg, #03a9f4, #00e676);
            padding: 8px 0;
            color: #fff;
            font-size: 0.9rem;
        }

        .top-contact-inner {
            max-width: 1140px;
            margin: 0 auto;
            padding: 0 12px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .top-contact-left span {
            display: inline-flex;
            align-items: center;
        }

        .top-contact-left i {
            margin-right: 6px;
        }

        .top-contact-right {
            display: flex;
            align-items: center;
        }

            .top-contact-right a.auth-link,
            .top-contact-right .user-greeting {
                color: #fff;
                margin-left: 15px;
                white-space: nowrap;
                text-decoration: none;
            }

            .top-contact-right a.social-icon {
                font-size: 1.2rem;
                margin-left: 15px;
                color: #fff;
            }

        .menu-principal .nav-link {
            text-transform: uppercase;
            font-weight: 600;
            font-size: 0.85rem;
            margin-left: 0.75rem;
            white-space: nowrap;
        }

            .menu-principal .nav-link.active {
                color: #00c853 !important;
            }

        .menu-principal .navbar-brand {
            display: flex;
            align-items: center;
        }

            .menu-principal .navbar-brand img {
                height: 40px;
            }
    </style>
</head>
<body class="has-fixed-nav">

<<<<<<< HEAD
    <p style="color:red; font-weight:bold;">ROL: @rol</p>

    <!-- TOP BAR -->
=======
>>>>>>> Sebas
    <div class="top-contact-bar">
        <div class="top-contact-inner">

            <div class="top-contact-left">
                <span><i class="fa-solid fa-phone"></i> +506 7263-7460</span>
                <span class="ms-3"><i class="fa-solid fa-envelope"></i> mundodehuellitascr@outlook.com</span>
            </div>

            <div class="top-contact-right">
                @if (isAuth)
                {
                    <span class="user-greeting">Hola, @User.Identity.Name</span>

<<<<<<< HEAD
                    @* ================== ADMIN ================== *@
                    if (esAdmin)
                    {
                        @Html.ActionLink("Mi perfil", "Perfil", "Usuario", null, new { @class = "auth-link" })

                        <form action="@Url.Action("Logout", "Account")" method="post" id="logoutForm" style="display:inline;">
                            @Html.AntiForgeryToken()
                            <a href="javascript:void(0);" class="auth-link"
                               onclick="document.getElementById('logoutForm').submit();">
                                Cerrar sesión
                            </a>
                        </form>

                    }
                    @* ================== EMPLEADO ==================
                                ESTABA DUPLICADO
                            else if (esEmpleado)
                            {
                                @Html.ActionLink("Mi perfil", "Perfil", "Usuario", null, new { @class = "auth-link" })
                                @Html.ActionLink("Cerrar sesión", "Logout", "Account", null, new { @class = "auth-link" })
                        }
                    *@
                    @* ================== EMPLEADO ================== *@
                    else if (esEmpleado)
                    {
                        @Html.ActionLink("Mi perfil", "Perfil", "Usuario", null, new { @class = "auth-link" })
                        <form action="@Url.Action("Logout", "Account")" method="post" style="display:inline;">
                            @Html.AntiForgeryToken()
                            <a href="javascript:void(0);" class="auth-link"
                               onclick="this.closest('form').submit();">
                                Cerrar sesión
                            </a>
                        </form>

                    }
                    @* ================== CLIENTE ================== *@
                    else
=======
                    @Html.ActionLink("Mi perfil", "Perfil", "Usuario", null, new { @class = "auth-link" })

                    if (esCliente)
>>>>>>> Sebas
                    {
                        <a class="auth-link" href="@Url.Action("Carrito", "Productos")" title="Carrito">
                            <i class="fa-solid fa-cart-shopping"></i>
                        </a>

                        @Html.ActionLink("Mis citas", "VerCitas", "Citas", null, new { @class = "auth-link" })
<<<<<<< HEAD
                        <form action="@Url.Action("Logout", "Account")" method="post" style="display:inline;">
                            @Html.AntiForgeryToken()
                            <a href="javascript:void(0);" class="auth-link"
                               onclick="this.closest('form').submit();">
                                Cerrar sesión
                            </a>
                        </form>
=======
>>>>>>> Sebas
                    }

                    <form action="@Url.Action("Logout", "Account")" method="post" id="logoutFormGeneral" style="display:inline;">
                        @Html.AntiForgeryToken()
                        <a href="javascript:void(0);" class="auth-link"
                           onclick="document.getElementById('logoutFormGeneral').submit();">
                            Cerrar sesión
                        </a>
                    </form>
                }
                else
                {
                    @Html.ActionLink("Iniciar sesión", "Login", "Account", null, new { @class = "auth-link" })
                }

                <a href="#" class="social-icon" aria-label="Facebook">
                    <i class="fa-brands fa-facebook-f"></i>
                </a>
                <a href="#" class="social-icon" aria-label="Instagram">
                    <i class="fa-brands fa-instagram"></i>
                </a>
            </div>
        </div>
    </div>

    <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm border-bottom menu-principal sticky-top">
        <div class="container">

            <a class="navbar-brand" href="@Url.Action("Index", "Home")">
                <img src="~/StaticFiles/img/Huellitas.png" alt="Logo" />
                <span class="ms-2 fw-bold">Mi Mundo de Huellitas</span>
            </a>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainMenu">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="mainMenu">
                <ul class="navbar-nav ms-auto">

                    @* ================= ADMIN ================= *@
                    @if (esAdmin)
                    {
<<<<<<< HEAD
                        <li class="nav-item">
                            <a class="nav-link" href="@Url.Action("Index", "Reportes")">REPORTES</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="@Url.Action("Index", "AdminUsuarios")">ADMIN USUARIOS</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="@Url.Action("Index", "AdminProductos")">ADMIN PRODUCTOS</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="@Url.Action("Index", "Comisiones")"> COMISIONES</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="@Url.Action("Index", "Marcaciones")">MARCACIONES</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="@Url.Action("Index", "Auditoria")">Auditoria</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="@Url.Action("VerMascotas", "Mascota")">ADMIN MASCOTAS</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="@Url.Action("Calcular", "Planilla")">PLANILLA</a>
                        </li>
=======
                        <li class="nav-item"><a class="nav-link" href="@Url.Action("Index", "Reportes")">REPORTES</a></li>
                        <li class="nav-item"><a class="nav-link" href="@Url.Action("Index", "AdminUsuarios")">ADMIN USUARIOS</a></li>
                        <li class="nav-item"><a class="nav-link" href="@Url.Action("Index", "AdminProductos")">ADMIN PRODUCTOS</a></li>
                        <li class="nav-item"><a class="nav-link" href="@Url.Action("Calcular", "Planilla")">PLANILLA</a></li>
                        <li class="nav-item"><a class="nav-link" href="@Url.Action("VerMascotas", "Mascota")">ADMIN MASCOTAS</a></li>
                        <li class="nav-item"><a class="nav-link" href="@Url.Action("Administrar", "Permiso")">ADMIN PERMISOS</a></li>
                        <li class="nav-item"><a class="nav-link" href="@Url.Action("Index", "Auditoria")">AUDITORÍA</a></li>
                        <li class="nav-item"><a class="nav-link" href="@Url.Action("LaboralIndex", "Personal")">PERSONAL</a></li>
                    }
                    else if (esEmpleado)
                    {
                        <li class="nav-item"><a class="nav-link" href="@Url.Action("Index", "Home")">INICIO</a></li>
                        <li class="nav-item"><a class="nav-link" href="@Url.Action("MisPermisos", "Permiso")">MIS PERMISOS</a></li>
>>>>>>> Sebas
                    }

                    @* ================= EMPLEADO ================= *@
                    else if (esEmpleado)
                    {
                        <li class="nav-item">
                            <a class="nav-link" href="@Url.Action("Index","Home")">INICIO</a>
                        </li>

                        <li class="nav-item">
                            <a class="nav-link fw-bold text-success"
                               href="@Url.Action("Index", "Marcaciones")">
                                MARCACIONES
                            </a>
                        </li>
                    }

                    @* ================= CLIENTE ================= *@
                    else
                    {
                        <li class="nav-item"><a class="nav-link" href="@Url.Action("Index", "Home")">INICIO</a></li>
                        <li class="nav-item"><a class="nav-link" href="@Url.Action("SobreNosotros", "Home")">SOBRE NOSOTROS</a></li>
                        <li class="nav-item"><a class="nav-link" href="@Url.Action("Equipo", "Home")">EQUIPO</a></li>
                        <li class="nav-item"><a class="nav-link" href="@Url.Action("Servicios", "Home")">SERVICIOS</a></li>
                        <li class="nav-item"><a class="nav-link" href="@Url.Action("Galeria", "Home")">GALERÍA</a></li>
                        <li class="nav-item"><a class="nav-link" href="@Url.Action("Productos", "Home")">PRODUCTOS</a></li>
                        <li class="nav-item"><a class="nav-link" href="@Url.Action("Blog", "Home")">BLOG</a></li>
                        <li class="nav-item"><a class="nav-link" href="@Url.Action("Contacto", "Home")">CONTÁCTENOS</a></li>

                        if (isAuth)
                        {
                            <li class="nav-item"><a class="nav-link" href="@Url.Action("Agendar", "Citas")">AGENDAR CITA</a></li>
                            <li class="nav-item"><a class="nav-link" href="@Url.Action("VerMascotas", "Mascota")">MIS MASCOTAS</a></li>
                        }
                    }

                </ul>
            </div>
        </div>
    </nav>

<<<<<<< HEAD

=======
>>>>>>> Sebas
    @RenderSection("FullWidthTop", required: false)

    <main class="container-fluid body-content px-3">
        @RenderBody()
        <hr />
        <footer class="text-center">
            <p>&copy; @DateTime.Now.Year - MiMundoHuellitas</p>
        </footer>
    </main>

    @RenderSection("FullWidthBottom", required: false)

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)

</body>
</html>